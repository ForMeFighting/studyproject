server:
  port: 8080
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    jedis:
      pool:
        max-active: 6 #最大连接数
        max-idle: 10 #最大空闲数
        min-idle: 2 #最小空闲数
    timeout: 2000 #超时时间